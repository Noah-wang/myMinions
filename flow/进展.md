# 进展日志（append，新的放最上面）

> 每个 Agent 处理完一棒，在最上面追加一条。顶部条目是当前交接棒。

## 2026-09-14 · 控制平面落进开源版 + 睡眠空壳修复 · Claude
- 做了什么：把 `/tmp/coros-commercial-worktree` 的控制平面逐文件核对后落进**开源版** `coros-running-agent`（提交 `8697ece`）；另修复睡眠晨报「起晚了收到一篇数据缺失报告、并且这一天再也收不到真报告」的问题（开源版 `4ee50cb`、myMinions `b4aa0cf`）。
- 为什么这么做：控制平面放不进 myMinions——`admin.py` 依赖 `runtime_settings.automation_payload`，而 myMinions 整个没有这套特性；服务器跑的也是开源版，Codex 实际是照着开源版源码写的。已与用户确认选「放开源版」。
- 怎么理解：**worktree 的 git diff 不能整份提交**。它混了两样东西：真正的控制平面改动，和「服务器快照 ≠ 本地仓库」的分叉噪音（49 文件 +2947/−2480，大部分是噪音）。按三类处理：纯新增 15 个文件直接拿；干净的控制平面改动整文件应用；`discord_bot.py`/`auto_report.py`/`sleep_report.py` 混了血只挑改动。myMinions 侧未提交的 OAuth 改动（`auth_flow.py`、`coros_mcp.py`、`auto_report.py`、`discord_bot.py`）**保持未提交**，因为同样的能力已经在开源版落地了，要不要反向同步回主仓是下一个决定。
- 产出（路径）：开源版 `src/runtime/tenant.py`、`control_store.py`、`identity.py`、`src/api/admin.py`、`agents/coros_report/auth_flow.py`、`web/admin.{html,js}`、`scripts/bootstrap_default_tenant.py`、6 个租户测试、`docs/COMMERCIAL_CONTROL_PLANE.md`。
- 问题 → 怎么解决：worktree 的 `discord_bot.py` 是 myMinions 血统，带 127 行 kitchen 命令而开源版没有 kitchen 能力，已删；它还把 Codex 的原始频道检查和 9/6 的论坛帖修复 AND 在一起，裸 `channel_id` 那次更严、等于撤销了修复（论坛帖里斜杠命令会被拒），已去重。`auto_report`/`sleep_report` 的 worktree 底子更旧，会退掉 `prompt_skills` 和 `runtime_settings`，只取了 MCP 授权提醒和 `allow_fallback`。睡眠那个洞的成因是发送前用 `_sleep_data_available`（只要有 "sleep" 字样就算数），而空壳**完全稳定**、稳定判定反而会主动放行，发完 `_mark_sent` 烧掉当天——改用 `_has_main_sleep`。
- 验证：开源版 `77 passed`；入口模块导入、默认租户解析（`default` → 原 `data/`）、`MULTI_TENANT_ENABLED=False` 均已确认。睡眠修复已部署线上并清掉 `2026-09-14` 的已发标记、窗口 12:00 延到 15:00，重启后日志显示已过 `_has_main_sleep` 并在数稳定次数。
- 下一步（交接棒）：**控制平面尚未部署到服务器**（线上仍是修复前的代码 + 今天单独部署的 `sleep_report.py`），部署前先备份并确认 `ADMIN_TOKEN`/`WEB_SETTINGS_TOKEN` 的配置边界。随后登录 `/admin` 给 `default` 租户绑定 Noah 的 Discord User ID，建一个测试租户验证独立 COROS OAuth 与跨租户隔离，通过后再开 `MULTI_TENANT_ENABLED=true`。另需决定 myMinions 侧那 4 个未提交的 OAuth 改动是提交、还是按「同步能力不同步文件」从开源版反向补。

## 2026-09-14 · COROS Running Agent 多用户控制平面与管理后台 · Codex
- 做了什么：在现有单用户服务上增加租户上下文、SQLite 控制数据库、平台身份绑定、订阅状态、按租户用量统计、COROS OAuth 目录隔离和 Discord 租户路由；增加受管理员令牌保护的 `/admin` 管理后台，可创建用户、修改套餐与订阅、绑定 Discord/飞书身份、查看 COROS 授权状态、模型用量、服务连接和审计记录。管理入口已从公开页面导航中移除。
- 为什么这么做：商业化时不应为每位用户复制整套 Agent 进程。当前方案复用 Bot、Web 和模型服务，同时把用户身份、COROS 授权、长期记忆、会话和用量按 `tenant_id` 隔离，降低部署和维护成本。
- 怎么理解：`default` 租户继续使用原有 `data/` 和 `~/.mcp-auth/`，因此旧功能无需迁移；新租户写入 `data/tenants/<tenant_id>/`，并使用独立 MCP OAuth 配置目录。控制数据库只保存租户、订阅、身份、用量和审计元数据，不保存 API Key、OAuth Token、运动数据或会话正文。线上 `MULTI_TENANT_ENABLED=false`，当前仍是兼容模式；先绑定主账号并完成测试，再打开多人路由。每租户自动运动/睡眠报告和飞书完整消息链路尚未完成。
- 产出（路径）：`src/runtime/tenant.py`、`src/runtime/control_store.py`、`src/runtime/identity.py`、`src/api/admin.py`、`src/api/web_server.py`、`src/bot/discord_bot.py`、`src/integrations/coros_mcp.py`、`agents/coros_report/auth_flow.py`、`web/admin.html`、`web/admin.js`、`web/styles.css`、`scripts/bootstrap_default_tenant.py`、`docs/COMMERCIAL_CONTROL_PLANE.md`、`docs/plans/2026-09-14-commercial-control-plane.md`；实现工作树为 `/tmp/coros-commercial-worktree` 的 `feature/commercial-admin` 分支，线上部署目录为 `/opt/agent`。
- 问题 → 怎么解决：本地仓库与线上源码差异较大，先把线上源码快照覆盖到独立 worktree 后再开发，避免覆盖用户未提交修改；管理令牌写入涉及安全边界，取得明确确认后才写入服务器 `.env`，令牌值未进入代码和文档；初始化脚本从绝对路径执行时找不到 `src`，已让脚本主动加入项目根目录；手机端宽表造成横向溢出，改为移动端分组清单。上线前建立了 `/home/ubuntu/backups/coros-running-agent-20260914-132503.tar.gz`。
- 验证：完整测试 `87 passed`；Python 编译和 `admin.js` 语法检查通过；桌面与 390px 手机视口完成浏览器验收；线上 `agentdeck-web.service` 和 `agentdeck-bot.service` 均为 active；`https://agent.noahwang.run/admin` 返回 200，`/api/admin` 无令牌返回 401、有效令牌返回 200。
- 下一步（交接棒）：先把 `/tmp/coros-commercial-worktree` 中本次控制平面改动与 `/Users/noahwang/代码/Agent/myMinions` 当前未提交修改逐文件核对并整理成正式提交；随后登录 `/admin` 给 `default` 租户绑定 Noah 的 Discord User ID，创建一个测试租户完成独立 COROS OAuth 与跨租户数据隔离测试。通过后再开启 `MULTI_TENANT_ENABLED=true`，下一阶段实现每租户自动报告投递目标和飞书事件/发消息适配。
