{
  "name": "natural_language_routing",
  "version": "1.0.0",
  "objective": "Evaluate whether MainAgentOrchestrator safely converts natural language messages into allowed internal commands.",
  "unit_under_test": "src.orchestrator.MainAgentOrchestrator",
  "dataset": "evals/datasets/natural_language_routing.json",
  "judge": "evals/judges/natural_language_routing.py",
  "metrics": {
    "route_accuracy": {
      "description": "Positive examples routed to the expected command and argument.",
      "threshold": 0.9
    },
    "rejection_accuracy": {
      "description": "Negative examples rejected with no route.",
      "threshold": 1.0
    },
    "cross_channel_rejection": {
      "description": "LLM responses that pick a command unavailable in the current Discord channel are rejected.",
      "threshold": 1.0
    },
    "low_confidence_rejection": {
      "description": "LLM responses below NATURAL_LANGUAGE_ROUTING_CONFIDENCE are rejected.",
      "threshold": 1.0
    },
    "invalid_argument_rejection": {
      "description": "Malformed kitchen action arguments are rejected.",
      "threshold": 1.0
    },
    "loop_tool_exposure": {
      "description": "Fraction of cases where the tool table the main-agent loop builds for a channel matches exactly. Permission now lives in the tool table: a write tool that never appears cannot be called. Exact-set matching, because both a missing read tool and an extra write tool are defects.",
      "threshold": 1.0
    },
    "flow_map_coverage": {
      "threshold": 1.0,
      "description": "暴露给模型的工具全部能映射到架构图模块；漏一个图就会说谎"
    },
    "prompt_scope_correctness": {
      "threshold": 1.0,
      "description": "系统提示里的能力描述和实际工具表一致；脱节时模型会照着提示词说自己做不到"
    }
  },
  "constraints": [
    "Do not call DeepSeek during this offline eval.",
    "Do not call Discord, COROS MCP, Bilibili, or the filesystem data stores.",
    "Evaluate the router post-processing and safety gates using fixed LLM route outputs.",
    "Running channel may only route to coros, running, feel, or feelings.",
    "Cooking channel may only route to kitchen.",
    "The ask route is only offered in channels whose capabilities actually contributed read-only tools, so a channel with no data sources cannot fall into open-ended answering.",
    "Loop-tool cases assert the exact tool set, not a subset: an extra write tool leaking onto the public read-only surface must fail loudly."
  ]
}
