[project]
name = "myminions"
version = "0.1.0"
description = "Personal multi-capability agent runtime."
requires-python = ">=3.13"
dependencies = [
    "apscheduler",
    "discord.py",
    "langgraph>=1.2.11",
    "mcp",
    "openai",
    "pypdf",
    "python-dotenv",
]

[tool.uv]
package = false
